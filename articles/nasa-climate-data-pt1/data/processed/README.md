# Processed Data Placeholder

This directory stores generated climate tables created by the notebook or API pipeline.

- No large processed files are committed by default.
- Regenerate locally from `notebooks/Climate_pt1.ipynb` or via `shared/nasa/data_access.py`.
